<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is allowed ONLY for the emulator's host loopback
  (10.0.2.2) and localhost, where the dev backend runs. All other traffic —
  i.e. any real deployment — must use HTTPS. Before a production release,
  point MYGUARDIAN_API_URL at an https:// origin and this config can be
  deleted entirely.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
